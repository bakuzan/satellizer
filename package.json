{
  "name": "satellizer",
  "version": "2.0.0",
  "description": "statistics ui",
  "proxy": "http://localhost:9003",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass-chokidar src/ -o src/",
    "watch-css": "npm run build-css && node-sass-chokidar src/ -o src/ --watch --recursive",
    "build": "npm run build-css && elm-app build",
    "post-build": "node ./rename-output.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bakuzan/satellizer.git"
  },
  "author": "Bakuzan",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bakuzan/satellizer/issues"
  },
  "homepage": "https://github.com/bakuzan/satellizer#readme",
  "dependencies": {},
  "devDependencies": {
    "node-sass-chokidar": "^1.3.4"
  }
}
